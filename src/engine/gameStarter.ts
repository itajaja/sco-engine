import Game from './Game';

/**
 * start a new game for the given players
 */
export function startNewGame(players: string[]): Game {
    // TODO
    return undefined;
}
/**
 * Assign the players to an empty map at the beginning of the game
 */
export function assignPlayersToMap(players, map): Sco.Model.GameState {
    // TODO
    return undefined;
}
