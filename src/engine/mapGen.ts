import * as Sco from '../typedefs/index';

/**
 * generate a map for the provided number of players
 */
export function generateMap(numOfPlayers: number): Sco.Map {
    // TODO
    return undefined;
}
